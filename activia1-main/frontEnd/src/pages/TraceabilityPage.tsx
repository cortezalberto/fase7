/**
 * TraceabilityPage - Page for Traceability Viewer feature
 * FIX 2.1: Create missing frontend pages (Cortez2 audit)
 */
import { TraceabilityViewer } from '../features/traceability/components/TraceabilityViewer';

export default function TraceabilityPage() {
  return <TraceabilityViewer />;
}
